package templates

templ Index(basePath string, siteName string) {
	@Layout("Медиатека", basePath, siteName) {
		<div
			id="content"
			hx-get="media"
			hx-trigger="load"
			hx-swap="innerHTML"
		></div>
	}
}
